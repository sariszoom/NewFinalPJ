doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel='stylesheet', href='/stylesheets/storage.css')
    title Storage 
  
  body 
    .container
      .main
        .user 
          h1 ชื่อ: #{nameUser.name}
          h1 ฝ่าย: นักศึกษา
        
        .navbar 
          a(href="/Index") ยืมคืน
          a(href="/ustorage") คลังอุปกรณ์

        .logout
          a(href="/logout") Logout 

          

      .page 

        .storage
          h1 คลังอุปกรณ์

        .title 
          h2 รูป
          h2 อุปกรณ์
          h2 จำนวน

        .inpageStorage 
          
            //- h2 รูป
            //- h2 ชื่อ
            //- h2 จำนวน
            each store in listStorage
              .equipmentData 
                img(src= store.pic)
                .nameEquipment
                  p1 #{store.name}
                .amountEquipment
                  p1 #{store.amount} อัน