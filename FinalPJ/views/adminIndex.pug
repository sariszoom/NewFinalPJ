doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(rel='stylesheet', href='/stylesheets/Index.css')
    title Index 
  <body>
    <div class="container">
        <div class="main">
            <div class="user">
                <h1>ชื่อ: #{admin}</h1>
                <h1>ฝ่าย: นักศึกษา</h1>

            </div>

            <div class="navbar">
                <a href="#">คลังอุปกรณ์</a>
                <a href="#">เพิ่ม-ลด อุปกรณ์</a>

            </div>
            <div class="logout">
                <a href="#">Logout</a>
            </div>  
        </div>

        <div class="page">
          <div class="storage">
            <h1>คลังอุปกรณ์</h1>
          </div>

          <div class="inpage">
            <div class="inputItem">
              <input type="text" name="itemamount" id="itemamount" placeholder="ใส่จำนวนอุปกรณ์ที่ต้องการเพิ่ม">
            
            </div>

            <div class="inputItem">
              <input type="text" name="itemamount" id="itemamount" placeholder="ใส่จำนวนอุปกรณ์ที่ต้องการเพิ่ม">
            
            </div>

            <div class="inputItem">
              <input type="text" name="itemamount" id="itemamount" placeholder="ใส่จำนวนอุปกรณ์ที่ต้องการเพิ่ม">
            
            </div>

          </div>
        </div>
    </div>
  </body>     
    //-     )
      
    //-   )
    //- )
    //- .container 
    //-   .main 
    //-     .user 
    //-       h1#name ชื่อ: #{user.name}











    //- h1= title
    //- p Welcome to #{title}
    //- if admin 
    //-   p #{admin}